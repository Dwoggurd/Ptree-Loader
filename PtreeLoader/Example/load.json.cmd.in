:; if [ -z 0 ]; then
  @echo off
  goto :WINDOWS
fi

: #-----------------------------------------------------------------------------
: # This script can be executed on both:
: # - Windows (cmd)
: # - Linux (shell)
: #
: # Loads example ptree file in JSON format
: #-----------------------------------------------------------------------------

: #-----------------------------------------------------------------------------
: # Linux
: #-----------------------------------------------------------------------------
param='Ptrees/root.json'
cmd="${CMAKE_BINARY_DIR}/PtreeLoader/Example/PtreeLoader"
echo -------------------------------------------------------------
echo Starting $cmd "$param"
echo -------------------------------------------------------------
$cmd $param
exit

: #-----------------------------------------------------------------------------
: # Windows
: #-----------------------------------------------------------------------------
:WINDOWS

set cmd="${CMAKE_BINARY_DIR}/PtreeLoader/Example/PtreeLoader.exe" "Ptrees/root.json"
echo -------------------------------------------------------------
echo Starting %cmd%
echo -------------------------------------------------------------
%cmd%
