:; if [ -z 0 ]; then
  @echo off
  goto :WINDOWS
fi

: #-----------------------------------------------------------------------------
: # This script can be executed on both:
: # - Windows (cmd)
: # - Linux (shell)
: #
: # Loads example ptree file in INFO format
: #-----------------------------------------------------------------------------

: #-----------------------------------------------------------------------------
: # Linux
: #-----------------------------------------------------------------------------
param='Ptrees/root.info'
cmd="${CMAKE_BINARY_DIR}/Ptree Loader/Example/PtreeLoader.exe"
echo -------------------------------------------------------------
echo Starting $cmd "$param"
echo -------------------------------------------------------------
$cmd $param
exit

: #-----------------------------------------------------------------------------
: # Windows
: #-----------------------------------------------------------------------------
:WINDOWS

set cmd="${CMAKE_BINARY_DIR}/Ptree Loader/Example/PtreeLoader.exe" "Ptrees/root.info"
echo -------------------------------------------------------------
echo Starting %cmd%
echo -------------------------------------------------------------
%cmd%
